---
_bootstrap_install:
  Alpine:
    raw: export LANG=C ; apk update ; apk add {{ bootstrap_packages }}
    stdout_regex: Installing
  Archlinux:
    raw: export LANG=C ; pacman -Sy --noconfirm {{ bootstrap_packages }}
    stdout_regex: ' installing python'
  Debian:
    raw: export LANG=C ; apt-get update && apt-get install -y {{
      bootstrap_packages }}
    stdout_regex: ' 0 newly installed'
  Gentoo:
    raw: export LANG=C ; equery l {{ bootstrap_packages }} || (emaint -a sync ;
      emerge -qkv {{ bootstrap_packages }} ; echo 'changed')
    stdout_regex: changed
  RedHat:
    raw: export LANG=C ; yum -y install {{ bootstrap_packages }}
    stdout_regex: Nothing
  Suse:
    raw: export LANG=C ; zypper -n install {{ bootstrap_packages }}
    stdout_regex: Nothing
_bootstrap_packages:
  Alpine: python3 sudo
  Amazon: python sudo
  Archlinux: python sudo
  CentOS_7: python sudo
  Debian: python3 sudo gnupg python3-apt
  Debian_10: python3 sudo gnupg2
  Debian_11: python3 sudo gnupg2
  Debian_12: python3 sudo gnupg2
  Debian_8: python sudo gnupg
  Debian_9: python sudo gnupg
  Fedora: python3 sudo python3-libdnf5 python3-rpm
  Gentoo: python sudo gentoolkit
  RedHat: python3 sudo
  RedHat_7: python sudo
  Suse: python3 python3-xml sudo
  Univention Corporate Server: python3 sudo gnupg python3-apt
bootstrap_facts_packages: '{{ _bootstrap_packages[ansible_facts.distribution ~''_''~ ansible_facts.distribution_major_version]|default(
  _bootstrap_packages[ansible_facts.distribution] )|default( _bootstrap_packages[ansible_facts.os_family]
  ) }}'
bootstrap_install: '{{ _bootstrap_install[bootstrap_distribution ~''_''~ bootstrap_distribution_major_version]|default(
  _bootstrap_install[bootstrap_distribution] )|default( _bootstrap_install[bootstrap_os_family]
  ) }}'
bootstrap_os_family_map:
  Alpine:
  - Alpine
  Archlinux:
  - Archlinux
  - Antergos
  - Manjaro
  Debian:
  - Debian
  - Ubuntu
  - Raspbian
  - Neon
  - KDE neon
  - Linux Mint
  - SteamOS
  - Devuan
  - Kali
  - Cumulus Linux
  - Pop!_OS
  - Parrot
  - Pardus GNU/Linux
  - Univention Corporate Server
  Gentoo:
  - Gentoo
  - Funtoo
  RedHat:
  - RedHat
  - Fedora
  - CentOS
  - Scientific
  - SLC
  - Ascendos
  - CloudLinux
  - PSBM
  - Rocky
  - OracleLinux
  - OVS
  - OEL
  - Amazon
  - Virtuozzo
  - XenServer
  - Alibaba
  - EulerOS
  - openEuler
  - AlmaLinux
  Suse:
  - SLED
  - openSUSE Tumbleweed
  - openSUSE Leap
  - SLES_SAP
  - SUSE_LINUX
  - SLES
  - openSUSE
  - SuSE
bootstrap_packages: '{{ _bootstrap_packages[bootstrap_distribution ~''_''~ bootstrap_distribution_major_version]|default(
  _bootstrap_packages[bootstrap_distribution] )|default( _bootstrap_packages[bootstrap_os_family]
  ) }}'
bootstrap_search:
  Archlinux: Arch Linux
  OracleLinux: Oracle Linux
  RedHat: Red Hat
